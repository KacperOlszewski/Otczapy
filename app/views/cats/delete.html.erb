<div class="hero-unit">
<h1>Usuń całą kategorię</h1>

<%= form_for(:category, :url => {:action => 'destroy', :id => @category.id}) do |pole| %>

<p >Czy napewno chcesz usunąć kategorię: <span class ="text-warning"><%= @category.name %> ?<span/></p>


<div>
	<%= submit_tag("Usuń",  {:class => "btn btn-mini btn-warning"}) %>
	<%= link_to("powrót", {:action=> 'index'}, {:class => "btn btn-mini btn-primary"}) %>
</div>

<% end %>

</div>