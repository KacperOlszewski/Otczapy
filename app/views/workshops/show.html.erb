<div class="hero-unit">

<h1>Pokaż workshopa</h1>

<table class="table table-striped table-bordered">

	<tr>
		<th>Nazwa</th>
		<td><%= @workshop.name %></td>
	</tr>
	<tr>
		<th>Pozycja</th>
		<td><%= @workshop.position %></td>
	</tr>
	<tr>
		<th>Miejsce</th>
		<td><%= @workshop.place %></td>
	</tr>
	<tr>
		<th>Data</th>
		<td><%= @workshop.date %></td>
	</tr>
	<tr>
		<th>Opis</th>
		<td><%= raw(@workshop.text) %></td>
	</tr>
	
	<% if !@workshop.img.blank? %>
		<tr>
			<th>Zdjęcie</th>
			<td><%= image_tag(@workshop.img, :class => "img-responsive") %></td>
		</tr>
	<% end %>	
	<tr>
		
		<th>Plik:</th>
			<% if !@workshop.file.blank? %> 
		        <td><%= link_to "Download", @workshop.file.url(:original) %></td>
			<% else %>
		        <td>brak</td>
			<% end %>
	</tr>


</table>

<%= link_to("Powrót", {:action => 'index'}, {:class => "btn btn-mini btn-primary"})%>


</div>
